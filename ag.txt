Template prompt chuẩn cho từng loại task:
Tạo module mới:
Tạo module [tên] theo đúng architecture trong CLAUDE.md.
Bao gồm: controller, service, repository, dto/, transformers/
Refer: docs/modules/[xx-tên].md để biết endpoints và transformer output cần tạo.
Thêm endpoint:
Thêm endpoint [METHOD] [/path] vào [module].
- Input DTO: [mô tả fields]
- Business logic: [mô tả ngắn]
- Transformer output: xem docs/modules/[xx].md phần "Transformer output"
Đảm bảo đúng layer: controller không gọi Prisma, transformer chỉ ở controller.
Thay đổi schema:
Thêm field [tên] kiểu [type] vào model [Model] trong schema.prisma.
Sau khi sửa schema, tạo migration file với tên mô tả rõ thay đổi.
KHÔNG dùng prisma db push.
Fix bug:
[Mô tả bug + error message + file liên quan]
Không thay đổi architecture. Không sửa file ngoài phạm vi bug này.
Những điều KHÔNG nên để Claude tự quyết:

Không để Claude tự chọn thêm package mới — hỏi bạn trước
Không để Claude refactor code ngoài phạm vi task
Không để Claude tự tạo migration mà không review SQL trước



Thứ tự bắt đầu code theo Sprint S1
1. PrismaModule + PrismaService
2. Common: ResponseInterceptor, HttpExceptionFilter, Guards
3. Module Auth (login, JWT, refresh token)
4. Module Employee (CRUD cơ bản)
5. Module Attendance (check-in/out)
6. Module Leave (submit + approve flow)
7. Module Notification (email queue)



-Thay thế các thẻ <select> <option> bằng components 'app\components\ui\Select.vue' thay vì dùng native, để UI đẹp hơn. Tương tự với mọi component date-picker.

Tạo trang quản lý phòng ban.

Dựa trên API: GET /v1/departments (xem docs/openapi.json → tag Departments)
Response shape: xem app/types/department.types.ts → DepartmentSummary[]

UI cần có:
- Bảng hiển thị danh sách phòng ban, số lượng nhân viên, trạng thái.
- Mỗi dòng trong bảng có menu dropdown gồm các buttons có icon và label là 'Chi tiết', 'Chỉnh sửa'.
- Pagination (dùng meta.totalPages từ API)
- Search input (query param: search)
- Button "Thêm phòng ban" trên table-header => trang 'departments/new' với form tạo mới
- Status badge: xanh (active), đỏ (disable)

Gọi API qua useDepartment() composable, không gọi $fetch trực tiếp.
Refer: CLAUDE.md phần API calls.